[tool.uv.workspace]
members = [
    "packages/qecdec",
    "packages/torchdecoder-core",
    "packages/qecbench",
    "benchmark-app",
    "torchdecoder",
]

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
select = ["UP"]
